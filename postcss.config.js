/**
 * 文件说明（是什么）：
 * - 本文件是「PostCSS 处理配置」。
 * - 定义 CSS 后处理插件链及其执行顺序。
 *
 * 设计原因（为什么）：
 * - 将样式构建能力集中管理，保证不同环境下样式输出稳定。
 * - 通过在该文件集中同类职责，避免逻辑分散，降低后续维护与排障成本。
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
